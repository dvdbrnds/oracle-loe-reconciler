version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - PORT=3001
      - DATABASE_PATH=/app/data/vendor-tracker.db
      - JWT_SECRET=${JWT_SECRET:-change-this-in-production}
      - DEFAULT_MONTHLY_HOURS=${DEFAULT_MONTHLY_HOURS:-100}
      - USE_MOCK_DATA=${USE_MOCK_DATA:-false}
      # Jira configuration
      - JIRA_INSTANCE_URL=${JIRA_INSTANCE_URL:-}
      - JIRA_API_EMAIL=${JIRA_API_EMAIL:-}
      - JIRA_API_TOKEN=${JIRA_API_TOKEN:-}
      - JIRA_PROJECTS=${JIRA_PROJECTS:-MOHEECI,MOCSO,MOCS,MOPT,MSPP}
      - JIRA_SYNC_INTERVAL_MINUTES=${JIRA_SYNC_INTERVAL_MINUTES:-15}
      # SAML/SSO configuration
      - SAML_ENABLED=${SAML_ENABLED:-true}
      - SAML_ENTRY_POINT=${SAML_ENTRY_POINT:-}
      - SAML_CALLBACK_URL=${SAML_CALLBACK_URL:-}
      - SAML_CERT=${SAML_CERT:-}
      - SESSION_SECRET=${SESSION_SECRET:-change-this-in-production}
    volumes:
      # Persist database and uploads
      - app_data:/app/data
      - app_uploads:/app/server/uploads
      - app_logs:/app/logs
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3001/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

volumes:
  app_data:
  app_uploads:
  app_logs:
